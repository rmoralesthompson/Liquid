# Liquid — Implementation Handoff

**Date:** 2026-08-02 · **For:** a fresh session continuing implementation. **v0.1 is code-complete and merged: #2–#13 are all done** (#12 landed via PR #28). **#29 (threat model) is drafted on PR #36 from `ticket-29-threat-model`, awaiting human signoff (`ready-for-human`); its audit spawned test-gap tickets #32–#35 (all ready-for-agent). Frontier: #26 (`*liquidDefer`, ready-for-agent), #32–#35, and — once PR #36 merges — #30 (the security-review pass, blocked on #29). #27 (parallel sibling OnInit, needs-triage) stays blocked on a human decision.** Do not start #27 without that decision.

> **Workflow (since #5):** a repo hook blocks pushing to `main` — work on a feature branch and open a PR (commit titles are hook-checked; the `Ticket #n:` / `Docs:` / `CI:` / `Chore:` prefixes in the log are the pattern).
>
> ⚠️ **Check PR state before branching.** `main` (local *and* origin) may lag the newest work while its PR is unmerged — verify with `gh pr list` and branch from the open PR's head (stacked), not from `main`, or you'll rediscover that mid-edit. As of this writing, #29's PR #36 (from `ticket-29-threat-model`) is open and unmerged; the human merges PRs.
>
> **Labels:** the `needs-triage` label now exists in the tracker (it was documented before it was created); `needs-info` and `ready-for-human` are still documented-but-uncreated — create them on first use.

## 1. What Liquid is (30 seconds)

A server-driven UI framework for Go: **Phoenix LiveView's runtime model + Angular's mental model, designed as a target for AI code generation.** Components are single Go structs paired with `.lsx` templates (HTML + Angular-style sugar), AOT-compiled to `html/template`. Interactivity is server-round-trip: a tiny fixed runtime posts events, the server patches HTML; push via SSE. The compiler doubles as the agent guardrail — structured diagnostics feed agent self-repair.

## 2. Where things stand

**Design is closed** (24 decisions, D1–D24, all settled — full text in [design-decisions.md](design-decisions.md); don't re-litigate). **Planning is published to the tracker:** [issue #1](https://github.com/rmoralesthompson/Liquid/issues/1) is the v0.1 spec (PRD, incl. the agreed two-seam testing model); **#2–#13** are tracer-bullet tickets with GitHub-native blocking edges (each also carries a `Blocked by` text fallback — keep both in sync if the graph changes).

**Tickets #2–#13 are done, reviewed, committed** (#2: `59a759f`; #3: `f4cf165`; #4: `4cb664b`; #5: `0c91d9b`; #6 via PR #14; #7 via PRs #15/#17; #8 via PR #19; #9 via PR #20; #10 via PR #21; #11 via PR #22; #13 via PR #24; #23 — a post-plan addition — via PR #25; all merged; #12 via the open PR #28). Code that now exists:

- `go.mod` — module `github.com/rmoralesthompson/liquid`, **`go 1.23.0`** (D24 floor — beware: `go get` likes to bump this; `golang.org/x/net` is pinned at `v0.38.0`, the last line compatible with 1.23).
- `cmd/liquid/main.go` — CLI skeleton, `build` verb, thin `run(args)` wrapper.
- `cmd/liquid/internal/compiler/` — the AOT compiler: pairs `foo.lsx` ↔ `foo.go` by filename, parses `.lsx` with `x/net/html` (**node tree, never regex** — invariant), rewrites `{{ Field }}` → `{{ .Field }}` in text nodes and attribute values, emits a generated `Template() string` method as `foo_gen.go` (gofmt'd via `go/format`). Note: emits generated Go rather than literal `go:embed` — a doc-sanctioned deviation, recorded in a comment on #2.
- **Diagnostics (#3, D13):** `Build(ctx, dir)`/`Vet(ctx, dir)` → `([]Diagnostic, error)` — user-fixable problems come back as diagnostics (`diagnostic.go`: D13 JSON contract, `Severity`/`Code` named types, codes LSX001 unclosed `{{` · LSX002 missing paired .go · LSX003 struct missing from package · LSX004 unknown field/method ref with Levenshtein "did you mean"); mechanical failures stay `error`. `scan.go` scans **raw source** for `{{ }}` positions (1-based line / byte col, pointing at the identifier, not the braces) — the transform itself stays node-tree, invariant intact. `vet.go` cross-checks refs via `go/types` using `golang.org/x/tools/go/packages` (**pinned v0.31.0** — later minors may bump the `go` directive past 1.23; check before upgrading). Component packages need a `go.mod` for `packages.Load` — fixtures each carry one. No `_gen.go` is written for a file with diagnostics. CLI: `liquid <build|vet> [dir] [--json]`; any diagnostic → non-zero exit; `--json` always emits an array (`[]` when clean); unknown flags/extra args rejected. Deferrals recorded on #4 (directive-level malformed-syntax diagnostics; structured diagnostics for a type-broken paired package).
- **Directives (#4):** `directives.go` — structural `*goIf`/`*goFor` compile to `{{if}}`/`{{range $x := …}}` via node-tree rewrites (control text inserted as RawNodes, never re-scanned); a raw-source directive scanner (in-tag state machine: skips comments/quoted values/prose, handles quoted/unquoted/valueless attrs, whitespace around `=`) supplies diagnostic positions. New codes: LSX005 malformed directive expression · LSX006 two structural directives on one element · LSX007 type-broken paired package (translated from `packages.Package.Errors` — `vet.go` now loads with `NeedSyntax|NeedTypesInfo` so type errors carry file:line:col). Directive expressions feed the LSX004 vet cross-check. Case-insensitive directive spelling is deliberate (recorded on #4). Quality deferrals (dispatch table, unify `scan.go` onto `cursor`) recorded on #7.
- `core/` — package `liquid`: `Component` interface (`Selector()`/`Template()`); `App` with `New(opts...)` (`WithLogger` for pluggable slog), `Route(path, c, opts...)` (template parsed **once, at registration**; prototype validated — non-nil reference-typed fields rejected; `pathParam` tags validated: exported string fields only in v0.1), `ServeHTTP` (GET/HEAD only, fresh prototype copy per request, buffered render).
- **Routing & lifecycle (#5):** `:param` segments bind via `pathParam` tags before `OnInit`; matching is on the **escaped** path with per-segment decode (`%2F` stays one segment), empty segments never match, one trailing slash tolerated, literal-beats-param at first differing position (registration order breaks exact ties). `guard.go`: `WithGuard` + `Allow`/`Deny` (403)/`Redirect` (302, D19), run **before instantiation**. `ctx.go`: `liquid.Ctx` embeds the request `context.Context`, accessors `Param`/`Query`/`Header` (D18). `Initializer` (`OnInit(ctx Ctx) error`); error → logged + clean framework error page (`errorPageHTML`); panics recovered to the same page, `http.ErrAbortHandler` re-panicked. Deferrals: Ctx test constructor → #7 · session accessor → #8 · dev-mode error page → #12.
- **DI, head, static (#6):** `inject.go` — `App.Provide` registers singletons by type; component fields tagged `inject:""` resolve at `Route` registration (concrete + interface matching; hard error on missing/ambiguous/typed-nil/unexported — never a nil field at request time, D8) and are copied into each fresh instance before param binding and `OnInit`. Dependency declaration is tag-based — a deliberate deviation from D8's any-matching-field wording, recorded with rationale on #6; `ProvideAs` (interface-keyed registration) deferred there too. `head.go` — every page renders inside a document shell (parsed once at package load); `HeadProvider` (`Head() liquid.Head`, title + meta, escaped, runs after `OnInit`) controls it, fallback title is the selector. `static.go` — `App.Static(dir)` mounts a stdlib file server at `/static/`; dir must exist at registration; `Cache-Control: public, max-age=3600` on hits only (a cached 404 would outlive the deploy that adds the file).
- **Hydro loop (#7):** compiler — `(click)="M"` → `data-liquid-action` + generated `Actions()` allowlist (D10); `[hydroId]` → `data-hydro-id="{{ .HydroID }}"`; new codes LSX008 (handler not `func()` — the `liquid.Event` variant is #8's) · LSX009 (no `HydroID string` field) · LSX010 (`(click)` with no `[hydroId]` in the file); `kinds.go` is the directive/binding dispatch table (one entry per kind: scan spelling, grammar check, vet ref, node rewrite — the #4 deferral, landed); `scanInterpolations` now rides `cursor`. Core — `hydro.go`: interactive render (component detected by `HydroID string` field) mints `liquid_session` cookie + crypto-random hydro token, registers the live instance; **bounded registry** (1024 sessions / 64 per session, FIFO eviction, cookie adopted only if it's a live registry key — LRU/GC/config/body-limits stay on #9); `POST /hydro-event` dispatches via registration-time method indexes (never reflection on client names), per-instance mutex, returns D19 `Envelope{patch|redirect}` carrying the **component render, not the shell**; `runtime.js` (embedded, served at `/liquid/runtime.js`, `<script>` tag in the shell) posts events and does the innerHTML swap with D21 focus preservation — browser-level verification deferred to #13 (comment there). `liquidtest/` — `New(t, app).Get(path)` → `Page.Text(sel)/HydroID()/Fire(action)` → `Patch{Code, Envelope, Text(sel)}`, cookie continuity, refusals assertable via `Code`; `liquidtest.Ctx(req, params)` (the #5 deferral) over new `liquid.NewCtx`. Design nuances + review outcomes recorded as comments on #7; deferrals on #8/#9/#13.
- **CSRF, `(submit)`, `liquid.Event` (#8):** compiler — `(submit)` in the kind table (`data-liquid-submit` + allowlist, `action: true` flag drives the generalized LSX010); every `<form>` gets the auto-injected hidden `csrf_token` input as a node-tree transform; LSX011 (`<form>` without `CSRFToken string`, detected by a tag-level `<form>` kind the tag scanner records); LSX008 accepts both D11 shapes (`func()` / `func(e liquid.Event)`, Event matched by import path — fixtures needing it carry a local `liquidstub` module their `go.mod` replace points at; `copyFixture` copies recursively via `os.CopyFS`). The #7-deferred refactors landed: `interpolation` → `structRef`, position clumps → `pos` type. Core — `csrf.go`: HMAC codec (`sessionID:expiry:signature`, per-process secret minted in `New`, **fixed 1h TTL — re-tie to the idle window in #9**; HMAC input is delimited `sessionID:expiry`, a recorded deviation on #8); renders mint the token into the `CSRFToken` field and a `liquid-csrf` shell meta tag (the click-path transport, documented in architecture.md §Security model); `/hydro-event` validates CSRF **before** registry/allowlist (403; order: cookie 404 → CSRF 403 → registry 404 → allowlist 404); `event.go`: `Event` embeds `Ctx`, `String`/`Int`/`Bind` (case-insensitive match, string+int, unmatched keys ignored), `Redirect` via a shared `eventReply` pointer → D19 redirect envelope skips the re-render; `resolveActions` resolves both shapes to an `action{idx, takesEvent}` table; `Ctx.Session()` (the #5 deferral, D18); `runtime.js`: submit listener (FormData → `payload`), `csrfToken` from the meta tag. `liquidtest` — `Fire(action, opts...)` sends the page token by default, `CSRF()`/`Field()` options for forged-token and form-payload cases, `Page.CSRFToken()`; a third tracer covers build-then-submit on the compiled form. The CSRF codec is white-box tested (`core/csrf_test.go`, clock control); everything wire-observable is at the seams.
- **Session hardening (#9):** `WithLimits(Limits{...})` App option — `MaxSessions` / `MaxComponentsPerSession` / `SessionIdleTimeout` / `MaxEventBytes`, zero fields keep documented defaults (1024 / 64 / 1h / 64 KiB), no unlimited setting. Registry eviction is **LRU at both levels** (`container/list`); idle sessions expire on touch and are swept at registration (**no background GC loop** — deliberately deferred to whoever builds D24 graceful shutdown; rationale on #9). `/hydro-event` refuses declared oversizes and bounds the body via `MaxBytesReader` (413) before decode. CSRF TTL now tracks the configured idle window (D15/D2). **D20.1 settled: dispatch mutex promoted per-instance → per-session** (`hydroSession.dispatch` serializes handler + re-render across all of a session's instances; proven by a blocking cross-instance test + 50-event hammer under `-race`). `App.now` is a replaceable clock for white-box expiry tests (`core/hardening_test.go`, same rationale as the CSRF codec tests). Two observations recorded on #9, not fixed: fixed-at-render CSRF expiry vs the now-sliding session window (D15 territory, needs the human, alongside the earlier D15 disclosure flag), and best-effort serialization across an eviction boundary.
- **SSE push + `BehaviorSubject` (#10):** `core/subject.go` — `BehaviorSubject[T]` (`Next`/`Subscribe`/`Value`, mutex-guarded; **Subscribe does not replay the current value** — `Value()` is the read path, a recorded deviation on #10); components declare bindings via `SubscriptionProvider` + `liquid.Observe(subject, apply)`, activated after registration (a subscriber without `HydroID` is a `Route` error). `core/sse.go` — `/hydro-sse` (GET, one stream per connection, per-session cap `Limits.MaxStreamsPerSession` default 8 with oldest-evicted, invariant-driven); per-entry **pump goroutine** (dirty-signal coalescing → render under the session dispatch mutex → fan-out; never blocks on emit, so handlers may emit mid-dispatch — the event's own patch reflects it via the shared `renderLocked` path); pumps/subscriptions die with their registry entry on every eviction/expiry path (`-race`-proven goroutine reaping in `core/sse_test.go`). **Connecting streams prime every session pump** (current-state push — closes the render→connect lost-emission window; D20.4-consistent); slow consumers (16-patch buffer) are disconnected, not served stale. `runtime.js`: EventSource, patch-by-hydroId, reload on re-`open` and on fatal `CLOSED` (dead-page-after-GC fix); browser-level verification of these paths deferred to #13. `liquidtest`: `Harness.Stream()` (in-process pipe-backed Flusher), `Stream.Next() *Push`/`Closed()`, `Push.Text(sel)`. Design nuances + review outcomes recorded as comments on #10.
- **Nesting + `[input]` (#11):** compiler — a hyphenated element is a child occurrence: `rewriteChildSelector` (node-tree) replaces it with `{{liquidChild "sel" "field" .Expr}}`; the tag scanner records child tags and their bracketed input attrs; selectors resolve against the package's `Selector()` **string literals** (same-package only in v0.1 — cross-package children false-LSX012, recorded on #11); new codes LSX012 (unknown selector, did-you-mean) · LSX013 (malformed expr / unknown child field, case-folded did-you-mean / unassignable via go/types) · LSX014 ((click)/(submit)/[hydroId] on a usage site — the element is replaced wholesale). Core — `registration` (selector-keyed render machinery) shared by routes and children; `App.Register` for child-only components (Route auto-registers; cross-type selector claim errors); child-bearing templates re-parse per render binding `liquidChild` to a `renderScope` (session established lazily — a static parent hosting an interactive child still mints cookie + shell CSRF meta after the buffered render); children instantiate per occurrence with DI + case-insensitive assignability-checked input copy, recurse behind a 32-level depth cap; only a child with `HydroID` mints a token and registers an entry, pumps/subscriptions included — plain children ride the parent's patch (D14). Lock order is one-way: session `dispatch` → registry `mu` (child registration during a patch re-render), never the reverse — documented on `hydroSession.dispatch`. Nuances recorded on #11: interactive-child re-registration churn (fresh token per parent re-render, entries bounded but churny; child state resets), children skip `OnInit` (input-driven projections), usage-site attrs/content dropped, cycle detection is the runtime depth cap only.
- **Example app + browser verification (#13):** `examples/dashboard/` — the D17 dashboard: `(click)` counter, SSE metric (subject injected via DI, seeded through an `[input]` because **children skip OnInit**), nested stat card (interactive child of a static parent — the design that dodges #11's churn nuance), CSRF'd `(submit)` renamer, guarded `/admin` (allow/redirect/deny one URL-edit apart). Components sit in `examples/dashboard/ui/` — the `liquid build` target — with wiring above in `package main`, because **a from-scratch build fails if code referencing components as `liquid.Component` shares their package** (vet type-checks before `Template()` exists → wall of LSX007s; recorded on #12 for the scaffolder). Generated `ui/*_gen.go` are committed; `cmd/liquid/example_test.go` rebuilds from a clean module slice (repo `go.mod`/`go.sum`/`core` + the example, temp-copied) and fails on drift. `liquidtest` gained `Fire`'s **`From(selector)`** option — multi-component pages need to aim events; default stays the document's first `data-hydro-id`. Browser verification (headless Chrome driving real `runtime.js` via a same-origin proxy harness; method + results in a comment on #13): D21 focus restore by id, pushes never steal focus/clobber typed input, `(submit)` FormData+meta-token path, reload-on-reconnect (exactly one reload, only after the transport returns). The fatal-CLOSED reload branch remains inspection-only. The example README carries the run commands and the D21 a11y checklist.
- **`Ctx.Fanout` concurrent OnInit loading (#23):** `core/fanout.go` — `ctx.Fanout(liquid.Load(&c.Field, fn, liquid.WithTimeout(d)), ...)` runs loaders concurrently under the request ctx (D18); the first error cancels the siblings and returns wrapped (`"fanout: %w"`, `errors.Is`-transparent) into the OnInit-error path; Fanout waits for every loader goroutine before returning (timeouts are cooperative — a loader that ignores its ctx delays the return, it is not killed); a loader panic re-raises on the caller. Review outcomes + deliberate non-changes are a comment on #23.
- **Scaffolder + dev loop (#12, D16):** `cmd/liquid/generate.go` — `liquid generate component <name> [dir]` (dir defaults to `ui/` — the #13 bootstrap shape) scaffolds a paired struct + `.lsx`; hyphenated lowercase selector required (hyphenless can never nest, D14), existing package clause adopted, overwrite refused. `cmd/liquid/dev.go` — `liquid dev [dir]`: stdlib polling watcher (300ms + settle debounce; `*_gen.go` excluded so the cycle never retriggers on its own output) → `compiler.Build` over every `.lsx` dir → `go build -tags liquiddev` → child restart (SIGTERM, 2s grace); template/app-build failures broadcast the D13 array over a localhost control stream (`LIQUID_DEV_CONTROL`, random-token URL) instead of restarting — `go build` stderr is translated to D13 as code `GO001`. Core's dev surface lives behind the **`liquiddev` build tag** (`core/dev_on.go` / `dev_off.go`; prod builds exclude it — pinned by `core/dev_off_test.go`): typed SSE frames, sessionless `?dev=1` broadcaster streams on `/hydro-sse` (bounded, 128), `core/dev.js` overlay/reload script injected into the shell, the app-side control relay, and the D18 **dev error page** (the #5 deferral, landed — escaped detail + stack on the 500). The success-path "reload push" is the SSE stream cycle itself: the restart drops streams and `runtime.js`/`dev.js` reload on reconnect (the #10/#13-verified contract); an explicit `reload` frame is supported end-to-end but deliberately not emitted mid-restart (double-reload race — recorded on #12). CI runs tests and lint in both tag flavors. Integration proof: `cmd/liquid/dev_integration_test.go` (real CLI binary + toolchain over a gen-less fixture app: bootstrap, edit→restart, break→LSX001 frame, fix→recover); #13's deferred criterion (the example under `liquid dev`) is now verified.
- **Editor tooling (#41):** `editors/vscode-lsx/` — the grammar-only VS Code extension gained an LSP client, backed by `liquid lsp` (`cmd/liquid/internal/lsp`): live LSX diagnostics, hover, completion, go-to-definition, and occurrence highlights over `.lsx` buffers. The server reuses the compiler's scanners and go/types vet via the exported analysis surface (`compiler.AnalyzeSource` / `LoadFacts` / `Facts.Vet` in `cmd/liquid/internal/compiler/analysis.go`) — one grammar, editor and `liquid vet` cannot disagree. Package facts are cached per directory against a `.go`/go.mod mtime fingerprint, so the keystroke path is scan+vet only. JSON-RPC framing is hand-rolled on stdlib (no new Go dependency); the extension needs `npm install` (vscode-languageclient) and degrades to grammar-only without the binary.
- Tests: 165 across 5 packages, all `-race` green — compiler seam (fixture → generated output; generated text executes as `html/template`), runtime seam (`httptest`: render, escaping, 404, 405, registration-time template errors, injected-logger 500 path, concurrency), and an **end-to-end tracer** (`cmd/liquid/tracer_test.go`: real `run(["build", dir])` → serve the generated template → assert HTML).
- CI (`.github/workflows/ci.yml`) activates on `go.mod` presence: golangci-lint (config is **`.golangci-lint.yml`** — nonstandard filename, passed via `--config`) + `go test -race`. Lint is strict: wrapcheck (wrap errors crossing package boundaries), revive `exported` (doc comments on all exported items).

## 3. How to work (the per-ticket pipeline)

1. **Work the frontier**: open tickets with zero open blockers. After #10, the frontier is **#11** immediately, plus **#12 and #13** once #10's PR merges. Claim by assigning: `gh issue edit <n> --add-assignee @me`.
2. Per ticket: `/tdd` → `/code-review` (dual-axis: Standards vs CLAUDE.md + smells; Spec vs the ticket's acceptance criteria — fetch with `gh issue view <n> --comments`, comments included) → fix findings → commit (`Closes #<n>`) → push.
3. Testing seams are **pre-agreed** (recorded in #1) — don't renegotiate: (1) the `liquid` CLI/compiler boundary: fixtures in → generated code + diagnostics out; (2) the HTTP runtime seam (later wrapped by `liquidtest`, ticket #7). Red before green; report pins (green-on-write tests) honestly.
4. Defer out-of-scope review findings by commenting them onto the owning ticket, not by expanding the slice.
5. Commands: `go build ./...` · `go test -v -race ./...` · `gofmt -l .` · `go vet ./...` · `golangci-lint run`.

## 4. The frontier tickets specifically

- **[#26 — `*liquidDefer`](https://github.com/rmoralesthompson/Liquid/issues/26)** (ready-for-agent): deferred components streamed over SSE — render a placeholder, stream the real subtree when its data lands. Unblocked now that #13 closed; design constraints and open questions are in the issue body. Natural building blocks already exist: the `event:`-typed SSE framing (`core/sse.go`), per-entry pumps, `Ctx.Fanout` for the loading side.
- **[#27 — parallel sibling OnInit](https://github.com/rmoralesthompson/Liquid/issues/27)** (needs-triage): **blocked on a human decision** — should children get OnInit at all, given v0.1 children are input-driven projections (#11)? Do not pick this up before the human weighs in; `Ctx.Fanout` (#23) already covers the intra-component concurrency story.
- **[#29 — threat model](https://github.com/rmoralesthompson/Liquid/issues/29)** (**ready-for-human**, drafted on PR #36): `docs/THREAT-MODEL.md` exists — five boundaries with code+pinning-test citations, the D13 prompt-injection posture, the D24 recommended CSP (previously promised, never written), the CLAUDE.md-invariant and REPORT §4 maps, #9's open D15 decisions, and accepted risks AR-1–AR-7. The human merging PR #36 is the signoff. The audit's gaps became **#32** (hydro token logged on SSE error paths + no log-hygiene pin — the one real finding), **#33** (production-boundary test gaps), **#34** (dev-loop test gaps), **#35** (`forbidigo` for D24's no-`fmt.Println`) — all ready-for-agent, independent, workable now.
- **[#30 — security-review pass](https://github.com/rmoralesthompson/Liquid/issues/30)** (ready-for-agent, **blocked by #29** — unblocks when PR #36 merges): adversarial pass over `core/`, `cmd/liquid/`, the runtime scripts against THREAT-MODEL.md + CLAUDE.md invariants; `/security-review` is the starting harness. The issue body carries the specific probes worth not losing (refusal order, SSE races, dev residue in prod builds, CLI path handling, GO001 sink chain, log hygiene). THREAT-MODEL.md asks it to re-verify the doc's `file:line` citations as its first step.
- Still open for whoever needs it: D24's App shutdown lifecycle remains unbuilt (the #9 idle-GC sweep deferral rides with it; `liquid dev` kills its child by signal today and did not need it).

## 5. Reading order for a fresh context

1. This file.
2. `gh issue view <frontier-ticket> --comments` — the ticket + any deferred findings.
3. [design-decisions.md](design-decisions.md) — for #26: D3, D14, D16, D20.
4. The code: `core/sse.go` + `core/dev_on.go` (the typed-frame transport and how a second frame kind rides it), `core/nesting.go` (child rendering #26 defers), `core/fanout.go` (the loading side), `core/hydro.go` (registry/pump lifecycle), `liquidtest/` (`Stream()` covers push assertions).
5. [architecture.md](architecture.md) §Interactivity + §Agentic pipeline; [CLAUDE.md](../CLAUDE.md) invariants.
6. [REPORT.md](REPORT.md) §4 and `docs/source/` — only for the "why" behind invariants; treat all source-doc code as untrusted.
